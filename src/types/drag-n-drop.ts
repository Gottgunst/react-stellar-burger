export type TUseDrag = { isDragging: boolean };
export type TUseDrop = {
  canDrop: boolean;
  isOver: boolean;
};
export type TDragIndex = {
  dragIndex: number;
  hoverIndex: number;
};
export type TDragSortId = {
  id: string;
  index: number;
};
